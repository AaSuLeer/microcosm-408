// Original teaching summaries, organized as expandable concept hierarchies.
const n=(title,children=[],tag='',link)=>({title,children,tag,link});
const f=(title,tag='')=>n(title,[],tag);
const sim=(title,link)=>n('进入仿真 · '+title,[],'实验',link);
export const knowledgeTree=[
n('01 计算机系统概述',[
 n('计算机的层次结构',[
  n('硬件五大部件',[f('运算器：执行算术与逻辑运算'),f('控制器：解释指令并发出控制信号'),f('存储器：保存程序和数据'),f('输入/输出设备：与外界交换信息')]),
  n('存储程序与程序执行',[f('指令和数据以二进制形式存放在存储器中'),f('PC给出取指地址；译码后决定本条指令的数据流向'),f('相同位串可表示指令或数据，取决于当前操作的解释','易错'),sim('CPU取指与执行','cpu')]),
  n('软件与硬件的分界 · ISA',[f('ISA约定指令、软件可见寄存器、寻址与机器行为'),f('编译器把高级语言翻译为该ISA的机器指令'),f('同一ISA可由不同数据通路和流水线实现','重点')])]),
 n('性能指标与计算',[
  n('CPU执行时间',[f('执行时间 = 指令数 × 平均CPI ÷ 主频','公式'),f('平均CPI = 各类指令CPI按动态指令占比加权'),f('10亿条指令、CPI=2、2GHz：执行时间为1秒','例题')]),
  n('比较机器性能',[f('相同任务的加速比 = 原时间 ÷ 新时间'),f('主频更高不保证更快；还取决于指令数和CPI','易错'),f('MIPS依赖指令复杂度，不宜跨ISA直接比较')]),
  n('字长与容量',[f('机器字长通常对应基本整数运算的数据宽度'),f('地址位数决定可区分地址数；容量还取决于编址单位'),f('32位地址按字节编址可区分2³²字节，即4GiB','例题')])])]),
n('02 数据的表示和运算',[
 n('数制、编码与转换',[
  n('无符号数与补码',[f('n位无符号数：0～2ⁿ−1；n位补码：−2ⁿ⁻¹～2ⁿ⁻¹−1','公式'),f('11111111作为无符号数是255，作为8位补码是−1','例题')]),
  n('扩展与截断',[f('无符号扩展高位补0；补码扩展复制符号位'),f('截断丢弃高位，可能改变数值；先确定目标位宽和类型','易错')]),
  n('移位',[f('逻辑右移补0；算术右移补符号位'),f('左移低位补0，但有符号运算可能溢出'),f('11111000算术右移一位为11111100：−8变为−4','例题')])]),
 n('算术逻辑单元 · ALU',[
  n('功能',[
   n('算术运算',[f('加、减；乘除可通过加减与移位迭代实现')]),
   n('逻辑运算',[f('与、或、非、异或；是否集成移位器由数据通路决定')]),
   n('功能选择',[f('多个功能结果由选择电路选出，控制器给出选择信号'),f('若需编码选择k种功能，至少需⌈log₂k⌉位','重点')])]),
  n('看懂ALU输入',[
   f('A、B：两个n位操作数，与该运算的数据宽度一致'),
   n('控制输入',[f('指定加、减、与、或等功能'),f('控制信号由指令译码、时序及条件共同产生')]),
   f('进位输入：接收低位进位；加减共用电路时可配合补码减法')]),
  n('看懂ALU输出',[
   f('F：n位运算结果；超出n位的部分不能直接留在F中'),
   n('状态标志',[f('CF：无符号进位；OF：有符号溢出'),f('ZF：结果为0；SF：结果最高位'),f('标志可送入状态寄存器，供条件转移使用','重点')]),
   f('进位输出：送往更高位或参与标志生成')]),
  n('全加器与进位链',[f('本位和 Sᵢ = Aᵢ XOR Bᵢ XOR Cᵢ','公式'),f('全加器接收Aᵢ、Bᵢ、Cᵢ，产生Sᵢ和Cᵢ₊₁'),f('串行进位链需要等待低位进位传播；先行进位通过逻辑并行求进位'),sim('选中位与进位传播','adder')])]),
 n('定点加减与标志',[
  n('补码减法',[f('A−B = A + NOT(B) + 1，按目标位宽保留结果'),f('减法CF与借位的约定依ISA而异，须读题设','易错')]),
  n('溢出判定',[f('同号相加结果变号，产生有符号溢出','重点'),f('127+1得到10000000：OF=1、CF=0','例题'),f('255+1的8位结果为0：CF=1；不能把CF等同于OF','易错')])]),
 n('定点乘法',[
  n('无符号移位相加',[
   n('初始化',[f('P清0，M保存被乘数，Q保存乘数；完整积预留2n位')]),
   n('每轮处理',[f('①检查Q最低位：1则P←P+M，0则跳过加法'),f('②M左移一位，Q逻辑右移一位；累计n轮')]),
   n('结果检查',[f('P为2n位乘积；13×6=78','例题'),f('本仿真采用M左移方案，不能套用P/Q联合右移表格','易错')]),sim('逐位检查、累加、移位','multiplier')]),
  n('有符号乘法与Booth',[
   f('补码乘法必须处理符号，不能直接沿用无符号部分积'),
   n('Booth相邻位判断',[f('Q₀Q₋₁=01：加被乘数；10：减被乘数'),f('00或11：不加减；随后进行算术右移'),f('需额外历史位Q₋₁；本系统尚未实现Booth动画','边界')])])]),
 n('定点除法',[
  n('概念与结果约束',[f('被除数A = 商Q × 除数B + 余数R','公式'),f('无符号除法且B>0时，应满足0≤R<B','重点'),
   n('商的位宽',[f('n位无符号数÷非零n位数，商可用n位保存'),f('2n位被除数÷n位除数，若只保留n位商，可能溢出'),f('有符号最小负数÷−1也可能溢出，不能套用无符号结论','易错')])]),
  n('开始前检查',[f('除数为0：不能执行普通除法，应报告异常条件'),f('若A<B，则Q=0、R=A，可直接得到结果'),f('双精度无符号除法：被除数高n位≥除数时，n位商容纳不下','重点')]),
  n('恢复余数法 · 每轮',[
   n('①移入下一位',[f('当前余数左移一位，并移入被除数的下一位')]),
   n('②试减除数',[f('T←R−B；暂存器需能表示负试减结果')]),
   n('③判断与写商',[f('T≥0：保留T为新余数，写商1'),f('T<0：恢复试减前余数，写商0')]),
   n('④结束检查',[f('处理完全部被除数位，读取商和余数'),f('13÷3得到Q=4、R=1；验算13=4×3+1','例题')]),sim('移入、试减、恢复、写商','divider')]),
  n('与其他除法方案的区别',[f('不恢复余数法根据余数符号选择加或减，最后可能需修正余数'),f('参考图中的额外商溢出检测轮属于特定双精度方案，不应机械增加到所有算法中','易错')])]),
 n('浮点表示与运算',[
  n('IEEE754单精度',[f('符号1位、阶码8位、尾数23位'),f('规格化值=(−1)ˢ×1.f×2^(E−127)','公式'),f('阶码全0与全1需分别考虑零、非规格化、无穷和NaN')]),
  n('浮点加减流程',[f('①对阶：小阶向大阶对齐，尾数右移'),f('②尾数加减 → ③规格化 → ④舍入 → ⑤判溢出'),f('对阶与舍入可能损失精度，不能认为浮点加法满足任意结合律','易错')]),f('浮点编码与运算动画尚未实现','边界')])]),
n('03 存储系统',[
 n('层次与器件',[
  n('局部性',[f('时间局部性：近期访问可能再次访问'),f('空间局部性：邻近地址可能很快被访问')]),
  n('SRAM与DRAM',[f('SRAM无需周期刷新，常用于Cache'),f('DRAM需刷新，常用于主存；存取时间与存储周期不同','易错')]),
  n('多模块交叉',[f('低位交叉：低地址位选模块，连续地址分散到不同模块'),f('4模块中地址0、1、2、3分别进入4个模块，地址4回到模块0','例题')])]),
 n('主存与CPU连接',[
  n('位扩展',[f('目标位宽÷芯片位宽，得到每组并行芯片数'),f('组内共享地址与片选，各片连接不同数据位','重点')]),
  n('字扩展',[f('目标字数÷芯片字数，得到互斥片选组数'),f('低位地址选片内字，高位译码选择芯片组','重点')]),
  n('综合例题',[f('1K×4组成4K×8：4组，每组2片，共8片'),f('低10位选字，高2位选组；不能把字数和位宽相加','题型')]),sim('芯片布局、片选与突发','memory')]),
 n('Cache',[
  n('地址拆分',[f('按字节编址时，块内偏移位数=log₂块字节数'),f('组号位数=log₂组数；其余为标记'),f('32位地址、16B块、64组：偏移4位、组号6位、标记22位','例题')]),
  n('三种映射',[f('直接映射：每组1行，块号mod行数定位'),f('组相联：块号mod组数定位组，再比较组内各行标记'),f('全相联：无组号，所有行都是候选')]),
  n('替换与写策略',[f('LRU依据最近使用顺序替换；FIFO依据装入顺序'),f('写回：修改Cache并置脏位，替换时写入下一级'),f('直写：写Cache时同时更新下一级；写未命中策略需另行确定')]),
  n('性能与边界',[f('平均时间=命中时间+未命中率×额外未命中代价','公式'),f('1ns命中、5%未命中、额外100ns：平均6ns','例题'),f('完整Cache替换与写策略动画尚未实现','边界')])]),
 n('虚拟存储器',[
  n('分页地址转换',[f('虚页号查询页表得到页框号，页内偏移不变'),f('256B页：VA=0x123，虚页1映射页框5，则PA=0x523','例题')]),
  n('TLB与缺页',[f('TLB缓存近期页表项，命中时减少查页表访存'),f('TLB未命中不等于缺页；缺页取决于页面是否在主存','易错')]),
  n('段式与段页式',[f('段式按逻辑段定位，需检查段界限'),f('段页式先定位段的页表，再按页转换；相应动画尚未实现','边界')]),sim('页表、TLB与缺页路径','paging')]),
 n('外部存储器',[
  n('磁盘几何',[f('盘面上的同心圆是磁道；相同半径的跨盘面磁道构成柱面'),f('磁头联动寻道，电路选择当前读写盘面')]),
  n('访问时间',[f('访问时间=寻道+旋转等待+传输时间'),f('7200rpm每转8.33ms，平均旋转等待约4.17ms','例题')]),
  n('SSD',[f('闪存按页读写、按块擦除，需要地址映射与垃圾回收'),f('SSD没有机械寻道，不能使用磁盘旋转时间公式','易错')]),sim('盘面、柱面与磁头','disk')])]),
n('04 指令系统',[
 n('指令格式',[
  n('操作码与地址码',[f('操作码确定做什么，地址字段定位操作数'),f('固定m位操作码最多编码2ᵐ种操作')]),
  n('扩展操作码',[f('少地址指令可用腾出的字段扩展操作码'),f('短操作码不能成为另一完整指令操作码的前缀','重点'),f('解题逐层扣除已使用前缀，再计算剩余编码数','题型')])]),
 n('寻址方式',[
  n('直接取得操作数',[f('立即：操作数就是字段A'),f('寄存器：操作数为指定寄存器内容')]),
  n('通过有效地址EA取数',[f('直接：EA=A；间接：EA=M[A]'),f('寄存器间接：EA=R；操作数均为M[EA]')]),
  n('地址相加形成EA',[f('基址：基址寄存器+位移；变址：基准地址+变址寄存器'),f('相对：题设PC基准+符号扩展位移'),f('A=12、R=64得到EA=76，但操作数是M[76]','例题')]),
  n('访存次数',[f('立即/寄存器通常不需额外数据访存，直接需一次，存储器间接通常需两次'),f('次数是否包含取指、Cache命中与页表访问，应按题目口径区分','易错')]),sim('八种寻址逐步执行','isa')]),
 n('机器级代码',[
  n('条件与循环',[f('比较产生标志；条件跳转据此决定下一条PC'),f('循环由条件判断、循环体与回跳组成')]),
  n('调用与返回',[f('CALL保存返回地址，再转移到入口'),f('RET恢复返回地址；向低地址增长的栈入栈使SP减小'),f('返回地址是调用后继续执行的位置，不是子程序入口','易错')]),sim('分支、CALL与RET','isa')]),
 n('CISC与RISC',[f('RISC常用规整格式、Load/Store结构；CISC可提供较复杂寻址和指令语义'),f('RISC不等于所有指令一拍完成，二者也不能仅按指令数量分类','易错')])]),
n('05 中央处理器',[
 n('CPU结构与寄存器',[
  n('运算器',[f('ALU执行运算，通用寄存器保存操作数与结果')]),
  n('控制器',[f('PC、IR及控制逻辑组织取指、译码与执行')]),
  n('访存寄存器',[f('MAR保存地址，MDR暂存读写数据'),f('内部寄存器不一定是ISA软件可见寄存器','易错')]),sim('CPU数据通路','cpu')]),
 n('指令执行过程',[
  n('取指',[f('PC→MAR；主存→MDR→IR；PC更新到后继地址')]),
  n('译码与执行',[f('根据操作码选择通路；算术、跳转与访存指令走不同操作序列')]),
  n('访存与写回',[f('LOAD读存储器后写目的寄存器；STORE向存储器写数据'),f('指令周期不等于一个时钟；教学微步骤也未必是物理时钟','易错')])]),
 n('数据通路与微操作',[
  n('单总线约束',[f('同一拍只能有一个源驱动共享总线','重点'),f('多个目的寄存器是否能同时接收由写使能决定')]),
  n('ADD操作分拍',[f('①R0out、Yin：保存第一个操作数'),f('②R1out、ADD、Zin：ALU求和并暂存'),f('③Zout、R0in：将结果写回')]),
  n('读图与填表',[f('先找源和目的，再标总线方向、ALU功能及写使能'),f('R0out与R1out不能同时驱动单总线，不能当成两个独立ALU输入','易错')]),sim('控制信号与单总线','control')]),
 n('控制器实现',[
  n('硬布线',[f('操作码译码+时序+条件，通过组合逻辑产生控制信号')]),
  n('微程序',[f('微地址寄存器定位控制存储器；微指令给出控制字'),f('顺序、分支或译码决定下一微地址')]),
  n('关键辨析',[f('机器指令属于ISA；微指令描述内部控制，二者不是同一层次','重点'),f('同一ISA可以采用不同控制实现')])]),
 n('异常与中断',[
  n('来源分类',[
   n('内部异常',[f('由当前指令执行引起，如非法指令、缺页、除法错误'),f('故障、陷阱和终止的返回位置与可恢复性需按具体异常判断')]),
   n('外部中断',[f('外设或定时器异步提出请求，通常在允许的指令边界响应')])]),
  n('响应与处理分工',[f('硬件响应：保存断点/状态，转向入口'),f('软件服务：保存要使用的通用寄存器，处理设备，恢复现场'),f('IRET等返回机制恢复PC与状态；请求有效不等于已经响应','重点')]),sim('请求到响应与返回','interrupts')]),
 n('指令流水线',[
  n('五级阶段',[f('IF取指 → ID译码读寄存器 → EX执行 → MEM访存 → WB写回'),f('理想k级流水执行n条指令需k+n−1拍；假设各级同拍且无暂停','公式')]),
  n('冒险分类',[
   n('结构冒险',[f('同拍争用同一硬件资源；可增加资源或安排暂停')]),
   n('数据冒险 · RAW',[f('后指令要读的数据尚未由前指令产生'),
    n('转发',[f('把结果直接送给使用者，绕过等待寄存器写回')]),
    n('LOAD-use',[f('LOAD数据在MEM末可用，紧邻使用者在EX需数据，典型五级仍须停一拍','重点')])]),
   n('控制冒险',[f('分支方向或目标尚未确定，顺序取指可能走错路径'),f('分支在EX判定时，需要冲刷已进入流水的错误路径指令')])]),
  n('时空图与性能',[f('逐拍标出每条指令所在阶段；暂停时检查前级是否也应冻结'),f('气泡是空操作；冲刷丢弃错误指令，不能计作完成指令','易错'),f('CPI=总时钟数÷完成指令数；不能机械取1','题型')]),sim('转发、暂停与冲刷','pipeline')]),
 n('并行处理基础',[
  n('并行类别',[f('SIMD：一条指令处理多份数据；MIMD：多个独立指令流')]),
  n('多核与多线程',[f('多核增加执行核心；多线程可能共享核心执行资源'),f('核心数翻倍不保证速度翻倍，串行部分与通信限制收益','易错')])])]),
n('06 总线',[
 n('总线分类与信号',[
  n('地址、数据、控制',[f('地址线定位对象，数据线搬运位串，控制线传递读写与握手'),f('控制线上的使能信号不是被搬运的数据值','易错')]),
  n('复用',[f('地址与数据分时复用同一组引脚，可减少引脚但增加传输阶段')])]),
 n('总线事务',[
  n('阶段',[f('请求/仲裁 → 地址 → 数据 → 应答 → 释放')]),
  n('同步与异步',[f('同步按统一时钟协调；异步通过请求与应答配合')]),
  n('突发传输',[f('一次地址阶段后连续传多个数据拍，摊薄地址开销'),f('1个地址拍+4个数据拍共5拍，数据拍效率4/5','例题')]),sim('授权与突发过程','io')]),
 n('带宽计算',[
  n('理论带宽',[f('每次传输位数×每秒传输次数，换算为字节时除以8','公式'),f('64位、每秒1亿次：800MB/s','例题')]),
  n('有效吞吐',[f('实际数据量÷总占用时间，需计地址、等待和仲裁等题设开销'),f('时钟频率不总等于每秒传输次数，要看每周期传几次','易错')])])]),
n('07 输入/输出系统',[
 n('接口与端口',[
  n('接口寄存器',[f('数据缓冲：暂存交换的数据'),f('状态寄存器：报告就绪、忙或错误'),f('控制寄存器：接收启动、方向等命令')]),
  n('端口译码',[f('地址和RD/WR共同决定访问哪个寄存器'),f('本例读0xF0取状态、写0xF0设控制；不同读写对象共用端口地址','例题'),f('端口地址复用与引脚分时复用不是同一概念','易错')]),sim('接口与寄存器复用','io')]),
 n('程序查询',[
  n('流程',[f('读状态 → 未就绪则循环 → 就绪后读写数据')]),
  n('开销',[f('轮询消耗CPU时间，即使此时没有有效数据传输'),f('每秒查询次数×每次查询CPU时间，得到查询占用','题型')])]),
 n('程序中断',[
  n('响应条件',[f('有请求、未被屏蔽、允许中断，且到达可响应时机'),f('屏蔽与优先级用于决定哪个请求可进入服务')]),
  n('处理链',[f('保存断点与状态 → 服务入口 → 保存寄存器'),f('处理设备并清请求 → 恢复现场 → 中断返回')]),
  n('多重中断',[f('高处理优先级请求可打断当前服务，需要允许嵌套并保存现场'),f('响应优先级与处理优先级不一定相同','重点'),f('当前仿真仅单次可屏蔽中断，未模拟嵌套','边界')]),sim('中断详细过程','interrupts')]),
 n('DMA',[
  n('初始化',[f('CPU设置方向、主存地址与传输计数')]),
  n('块传输',[f('DMA请求总线，获得授权后直接在设备和主存之间搬运'),f('每次搬运更新地址并递减计数')]),
  n('结束',[f('计数到0后释放总线，通知CPU处理完成')]),
  n('与中断方式比较',[f('程序中断通过CPU指令搬运；DMA搬运绕过CPU通用寄存器','重点'),f('DMA仍需CPU初始化与收尾，不能理解为CPU完全不参与','易错')]),sim('DMA仲裁与搬运','io')])])
];
// Priority is an editorial study recommendation, not an official score ranking.
export const syllabusReview={year:2026,checked:'2026-09-12',source:'https://kaoyan.xdf.cn/202511/15003718.html',status:'公开转载；未核实2027官方全文'};
const priorityTopics=new Set(['CPU执行时间','软件与硬件的分界 · ISA','无符号数与补码','算术逻辑单元 · ALU','定点加减与标志','定点乘法','定点除法','浮点表示与运算','主存与CPU连接','Cache','虚拟存储器','指令格式','寻址方式','机器级代码','数据通路与微操作','控制器实现','异常与中断','指令流水线','带宽计算','接口与端口','程序中断','DMA']);
function annotate(nodes){for(const node of nodes){if(priorityTopics.has(node.title)&&!node.tag)node.tag='重点';annotate(node.children);}}
annotate(knowledgeTree);
function identify(nodes,prefix=''){nodes.forEach((node,i)=>{node.key=prefix?`${prefix}-${i}`:`${i}`;identify(node.children,node.key);});}
identify(knowledgeTree);
