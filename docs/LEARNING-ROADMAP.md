# 学习系统迭代与验收

顺序固定：5 → 6 → 1 → 2 → 3 → 4。每个模块须完成自动测试与浏览器验收后才能推进下一模块。

| 模块 | 范围 | 验收条件 | 状态 |
| --- | --- | --- | --- |
| 5 学习导航 | 搜索、章节聚焦、重点筛选、持久学习进度；知识/实验/例题/错题入口 | 搜索定位深层节点；筛选正确；刷新保留进度；尚无练习时如实提示 | 已验收：搜索/筛选/深层定位/章节聚焦/刷新保留进度/资源入口；浏览器测试进度已清除 |
| 6 模型约定 | 每实验位宽、符号、时钟、算法与存储假设；算法比较 | 约定与引擎一致，比较明确适用条件 | 已验收：14页约定、参数同步及除法/乘法/控制/I/O对照 |
| 1 逐步追踪 | 可点击值与控制信号；来源/处理/去向；前后对照、回退慢放 | 中间值可推导，与动画和回退状态一致 | 已验收：14页逐步快照；算术操作前后值；2D/3D点击；回退、0.5倍重播；10组自动测试通过 |
| 2 补齐实验 | 浮点表示/加减；Cache映射/替换/写策略；编码/大小端；多重中断；可修改短程序控制/流水线 | 数值与边界测试、模式对比、逐步动画均通过 | 未开始 |
| 3 整机联动 | 一条LOAD贯穿取指、译码、EA、TLB、Cache、主存、写回；局部返回保留状态 | 同一机器状态贯穿路径；局部展开不启动独立状态 | 未开始 |
| 4 练习复盘 | 关键步骤先答后播，解释错误并记录错题 | 答案判定正确；复盘可追踪；导航连接真实错题 | 未开始 |

用量任一窗口剩余低于5%时，完成当前最小修改和验证后停止，记录断点。不得以“已建数据结构”代替模块完成。

## 续做断点（2026-09-12）

已按顺序验收 5 → 6 → 1；下一模块为 2，尚未开始，之后再进入 3 和 4。
模块1浏览器实测：默认7÷5首轮试减R由0变−5，恢复由−5变0，回退重新显示0→−5；0.5倍重播进入选位动画；3D的R标签可选择同一字段；加法器状态按钮可选择逐位和。控制台无错误。算术追踪使用操作前后值，其他实验明确标成相邻教学步骤快照，不能理解为完整电气信号模型。
剩余优化：加法器状态栏的最终和/标志需与“本步部分和”更明确区分，避免误认当前位已算完所有位。模块2仍须逐项完成浮点、Cache、编码/大小端、多重中断与可编辑程序，并验收整个模块后再推进。
