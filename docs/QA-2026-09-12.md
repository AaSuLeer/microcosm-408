# 2026-09-12 续查记录

## 修复

- CPU三维逻辑总线没有实体网格，空包围盒导致连线坐标无效，继而停止动画循环。逻辑节点现在使用有限尺寸端口；回归测试覆盖两个方向和同节点连接的全路径采样。
- 三维顶部动作说明按实际高度预留标签空间，避免窄窗口换行后压到标签。
- 算术移动数字框随数字长度扩展；二维数字沿显示的折线路径移动。除法移入位绕开除数寄存器；三维乘法加法阶段从被乘数寄存器取操作数。
- 除法公式分行显示，1440像素桌面布局下无需横向滚动。
- 621～850像素宽度使用顶部横向导航，给图形留出完整宽度。
- 内部模块统一版本，避免浏览器混用缓存版本。

## 验证范围

- 前次已检查全部13个部件的默认二维正文；修复多张流程图的行距和英文拆词。
- 本次补齐剩余10个三维页面；结合前次整机、内存、磁盘、I/O，14页默认三维标签均已检查。CPU首次检查发现故障，修复后再次逐页检查无标签重叠。
- 自动渲染检查覆盖90组模式/边界输入的全部教学步骤：寻址、分支/调用、控制器、流水线、屏蔽、扩展、I/O、缺页、磁盘边界、触发器及运算边界。
- 浏览器抽查CALL现场、无转发流水线、字扩展、查询/中断I/O、缺页、中断屏蔽、255×255、除法移入动画；抽查状态未发现二维正文相互覆盖。
- 检查1440×1000与800×900布局：除法公式无水平溢出，窄窗口无页面水平溢出，三维标签未相互覆盖。
- 自动测试七个文件全部通过。测试中的渲染检查不等于全部输入组合的逐像素视觉验证。

## 保留的模型边界

未新增浮点、完整Cache、交叉存储时序、嵌套中断等未实现仿真；导图仍明确标出缺口。三维是教学结构模型，不是电气或真实器件时序模拟。不能据本次检查宣称任意相机姿态、任意输入和任意屏幕都经过穷举视觉验证。
